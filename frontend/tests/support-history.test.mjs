import test from 'node:test';import assert from 'node:assert/strict';import {supportTimeline,hasUnreadSupport} from '../app/lib/studio/support-history.ts';
test('staff replies are kept in conversation order and legacy reply is not duplicated',()=>{const c={transcript:[{role:'user',content:'first',created_at:'2026-09-05T10:00Z'},{role:'user',content:'follow up',created_at:'2026-09-05T10:02Z'}],staff_reply:'second reply',staff_replies:[{id:'a',content:'first reply',created_at:'2026-09-05T10:01Z'},{id:'b',content:'second reply',created_at:'2026-09-05T10:03Z'}]};assert.deepEqual(supportTimeline(c).map(m=>m.content),['first','first reply','follow up','second reply']);assert.equal(supportTimeline({staff_reply:'legacy'}).length,1);});
test('acknowledging an older reply cannot clear a newer notification',()=>{assert.equal(hasUnreadSupport({last_staff_at:'2026-09-05T10:03Z',customer_seen_at:'2026-09-05T10:02Z'}),true);assert.equal(hasUnreadSupport({last_staff_at:'2026-09-05T10:03Z',customer_seen_at:'2026-09-05T10:03Z'}),false);assert.equal(hasUnreadSupport(null),false);});
